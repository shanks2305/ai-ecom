PORT=5000
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_ecom?schema=public"
INITIAL_SUPER_ADMIN_EMAIL=admin@example.com
INITIAL_SUPER_ADMIN_FIRST_NAME=admin
INITIAL_SUPER_ADMIN_LAST_NAME=john

# Email (optional in development — OTPs are logged if SMTP is not set)
# With Mailpit: run `npm run db:up` and open http://localhost:8025
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM="AI Ecom <noreply@ai-ecom.local>"
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASSWORD=